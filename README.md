# 本当に最小のAI - tiny.py 解説

## 全体の流れ

```
文字 → ベクトル(数値の列) → 次の文字を予測 → 答え合わせ → 重み調整
これを300回繰り返して賢くなる → 学んだ知識で文字を生成する
```

---

## 1. モデルの部品（26-27行目）

```python
emb = nn.Embedding(vocab, 8)   # 9種の文字 → 8次元のベクトルに変換
net = nn.Linear(8, vocab)      # 8次元のベクトル → 9種の文字のスコアに変換
```

コンピュータは文字を直接理解できないので、数値の列（ベクトル）に変換する必要がある。

- **emb**: 「も」→ `[0.3, -0.1, 0.7, ...]` のような8個の数値に変換する辞書。最初はランダムな値で、学習で調整される
- **net**: 8個の数値を受け取り、「次の文字が う/か/た/は/ま/も/ら/れ/ろ である確率」を9個の数値（スコア）として出す

```
文字 → emb → 8次元ベクトル → net → 9個のスコア → 一番高いのが予測
```

---

## 2. 損失関数とオプティマイザ（28-29行目）

```python
loss_fn = nn.CrossEntropyLoss()
opt = torch.optim.SGD(..., lr=1.0)
```

- **loss_fn（損失関数）**: 予測と正解のズレを1つの数値で表す。予測が正解に近ければ小さく、外れていれば大きくなる
- **opt（オプティマイザ）**: 「どの方向にどれだけ重みを動かせばlossが下がるか」を計算して、実際に重みを更新する仕組み。`lr=1.0`は1回の更新の大きさ

---

## 3. 学習ループ（35-42行目）

```python
for step in range(300):
    pred = net(emb(X))          # ① 予測
    loss = loss_fn(pred, Y)     # ② 正解との誤差
    opt.zero_grad()             # ③ 前回の勾配をリセット
    loss.backward()             # ④ 誤差逆伝播
    opt.step()                  # ⑤ 重み更新
```

これを300回繰り返す。1回のループで起きること:

1. **予測**: 全入力文字をベクトルにして、次の文字のスコアを出す
2. **誤差計算**: 予測スコアと正解を比べて「どれだけ間違っているか」を数値化
3. **勾配リセット**: 前回の計算結果を消す（毎回必要なお約束）
4. **逆伝播**: 「embとnetの各パラメータをどう変えればlossが下がるか」を計算する。これが **勾配**
5. **更新**: 勾配の方向にパラメータを少し動かす

繰り返すと loss が `2.5 → 0.4` と下がっていく = 予測が上手くなっていく。

---

## 4. 生成（46-54行目）

```python
ch = "も"
for _ in range(15):
    x = torch.tensor([to_i[ch]])                    # 今の文字を数値に
    logits = net(emb(x))                             # 次の文字のスコアを予測
    probs = torch.softmax(logits, dim=-1)            # スコアを確率に変換（合計1.0）
    ch = to_c[torch.multinomial(probs, 1).item()]    # 確率に従って1文字選ぶ
    result += ch
```

- **softmax**: スコア `[2.0, 0.5, -1.0, ...]` を確率 `[0.7, 0.15, 0.03, ...]` に変換する（全部足すと1.0になる）
- **multinomial**: その確率に従ってサイコロを振るように1文字選ぶ。確率0.7の文字は70%の確率で選ばれる

これを繰り返すことで「も→も→た→ろ→う→...」と文字が生成される。

---

## モデルの規模

| 項目 | 値 |
|------|-----|
| パラメータ数 | 153 |
| 学習データ | 14文字 |
| 語彙数 | 9種 |
| 層の数 | 2（Embedding + Linear） |
